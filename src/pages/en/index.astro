---
import PageLayout from '../../layouts/PageLayout.astro';
import SEOHead from '../../components/astro/SEOHead.astro';
import LogoBar from '../../components/astro/LogoBar.astro';
import StatsCounter from '../../components/astro/StatsCounter.astro';
import FeatureCard from '../../components/astro/FeatureCard.astro';
import CTASection from '../../components/astro/CTASection.astro';
import FAQ from '../../components/astro/FAQ.astro';
import MascotteSection from '../../components/astro/MascotteSection.astro';
import ScrollReveal from '../../components/animations/ScrollReveal.astro';
import HeroPhone3D from '../../components/react/HeroPhone3D.tsx';
import { t } from '../../i18n/utils';
import { generateOrganizationSchema, generateWebSiteSchema } from '../../lib/seo';

const locale = 'en';

// Hero
const heroTitle = await t(locale, 'home', 'hero.title');
const heroSubtitle = await t(locale, 'home', 'hero.subtitle');
const heroBaseline = await t(locale, 'home', 'hero.baseline');
const heroCta = await t(locale, 'home', 'hero.cta-primary');
const heroCtaSecondary = await t(locale, 'home', 'hero.cta-secondary');
const heroMicroText = await t(locale, 'home', 'hero.micro-text');

// Stats
const statsTitle = await t(locale, 'home', 'stats.title');

// Benefits
const benefitsTitle = await t(locale, 'home', 'benefits.title');
const benefits = [];
for (let i = 0; i < 4; i++) {
  benefits.push({
    title: await t(locale, 'home', `benefits.items.${i}.title`),
    subtitle: await t(locale, 'home', `benefits.items.${i}.subtitle`),
    description: await t(locale, 'home', `benefits.items.${i}.description`),
    proof: await t(locale, 'home', `benefits.items.${i}.proof`),
  });
}

// Offers
const offersTitle = await t(locale, 'home', 'offers.title');
const offers = [];
for (let i = 0; i < 3; i++) {
  offers.push({
    title: await t(locale, 'home', `offers.items.${i}.title`),
    description: await t(locale, 'home', `offers.items.${i}.description`),
    cta: await t(locale, 'home', `offers.items.${i}.cta`),
    link: await t(locale, 'home', `offers.items.${i}.link`),
  });
}

// How it works summary
const hiwTitle = await t(locale, 'home', 'how-it-works-summary.title');
const hiwCta = await t(locale, 'home', 'how-it-works-summary.cta');
const hiwSteps = [];
for (let i = 0; i < 4; i++) {
  hiwSteps.push({
    title: await t(locale, 'home', `how-it-works-summary.steps.${i}.title`),
    description: await t(locale, 'home', `how-it-works-summary.steps.${i}.description`),
  });
}

// Social proof
const socialTitle = await t(locale, 'home', 'social-proof.title');
const partnersSubtitle = await t(locale, 'home', 'social-proof.partners.title');
const techPartners = [];
for (let i = 0; i < 4; i++) {
  techPartners.push({
    name: await t(locale, 'home', `social-proof.partners.items.${i}.name`),
    description: await t(locale, 'home', `social-proof.partners.items.${i}.description`),
  });
}

// Mascotte
const mascotteText = await t(locale, 'home', 'mascotte.text');
const mascotteCta = await t(locale, 'home', 'mascotte.cta');

// FAQ
const faqTitle = await t(locale, 'home', 'faq.title');
const faqItems = [];
for (let i = 0; i < 4; i++) {
  faqItems.push({
    question: await t(locale, 'home', `faq.items.${i}.question`),
    answer: await t(locale, 'home', `faq.items.${i}.answer`),
  });
}

// Final CTA
const finalCtaTitle = await t(locale, 'home', 'final-cta.title');
const finalCtaSubtitle = await t(locale, 'home', 'final-cta.subtitle');
const finalCtaPrimary = await t(locale, 'home', 'final-cta.cta-primary');
const finalCtaSecondary = await t(locale, 'home', 'final-cta.cta-secondary');

// SEO
const metaTitle = await t(locale, 'home', 'meta.title');
const metaDescription = await t(locale, 'home', 'meta.description');
const orgSchema = generateOrganizationSchema();
const siteSchema = generateWebSiteSchema(locale);
---

<PageLayout title={metaTitle} description={metaDescription} locale={locale}>
  <SEOHead slot="head" locale={locale} page="home" jsonLd={[orgSchema, siteSchema]} />

  <!-- ========== 1. HERO ========== -->
  <section class="relative overflow-hidden bg-gradient-to-br from-[var(--color-green-primary)]/10 via-transparent to-transparent">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-24 lg:py-32">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
        <ScrollReveal direction="left">
          <div class="max-w-xl">
            <span class="inline-block text-sm font-semibold text-[var(--color-green-primary)] tracking-wide uppercase mb-4">
              {heroBaseline}
            </span>
            <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-[var(--text-primary)] leading-tight tracking-tight mb-6">
              {heroTitle}
            </h1>
            <p class="text-lg md:text-xl text-[var(--text-secondary)] leading-relaxed mb-8">
              {heroSubtitle}
            </p>
            <div class="flex flex-col sm:flex-row gap-4">
              <a href="/en/contact" class="inline-flex items-center justify-center bg-[var(--color-green-primary)] text-white px-8 py-4 rounded-xl font-semibold hover:bg-[var(--color-green-dark)] transition-colors text-center">
                {heroCta}
              </a>
              <a href="/en/contact" class="inline-flex items-center justify-center border-2 border-[var(--color-green-primary)] text-[var(--color-green-primary)] px-8 py-4 rounded-xl font-semibold hover:bg-[var(--color-green-primary)] hover:text-white transition-colors text-center text-sm">
                {heroCtaSecondary}
              </a>
            </div>
            <p class="mt-3 text-sm text-[var(--text-secondary)] opacity-70">{heroMicroText}</p>
          </div>
        </ScrollReveal>

        <ScrollReveal direction="right" delay={0.2}>
          <div class="w-full h-[400px] md:h-[500px] lg:h-[600px]">
            <HeroPhone3D client:only="react" />
          </div>
        </ScrollReveal>
      </div>
    </div>
    <div class="absolute top-1/2 right-0 -translate-y-1/2 w-[600px] h-[600px] bg-[var(--color-green-primary)]/5 rounded-full blur-3xl pointer-events-none" aria-hidden="true"></div>
  </section>

  <!-- ========== 2. STATS ========== -->
  <div class="bg-[var(--bg-beige)]">
    <ScrollReveal>
      <StatsCounter locale={locale} title={statsTitle} />
    </ScrollReveal>
  </div>

  <!-- ========== 3. BENEFITS ========== -->
  <section class="py-16 md:py-24 bg-[var(--bg-green)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <ScrollReveal>
        <div class="text-center mb-12 md:mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-[var(--text-primary)] tracking-tight">
            {benefitsTitle}
          </h2>
        </div>
      </ScrollReveal>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {benefits.map((b, i) => (
          <ScrollReveal delay={i * 0.1}>
            <div class="bg-[var(--bg-primary)] rounded-2xl p-8 border border-[var(--border-color)] h-full">
              <p class="text-sm font-semibold text-[var(--color-green-primary)] mb-2">{b.subtitle}</p>
              <h3 class="text-xl font-bold text-[var(--text-primary)] mb-3">{b.title}</h3>
              <p class="text-[var(--text-secondary)] leading-relaxed mb-4">{b.description}</p>
              <p class="text-sm text-[var(--color-green-primary)] italic">{b.proof}</p>
            </div>
          </ScrollReveal>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== 4b. LOGO BAR ========== -->
  <div class="bg-[var(--bg-beige)]">
    <LogoBar locale={locale} bg="beige" />
  </div>

  <!-- ========== 5. VERTICALS ========== -->
  <section class="py-16 md:py-24 bg-[var(--bg-beige)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <ScrollReveal>
        <div class="text-center mb-12 md:mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-[var(--text-primary)] tracking-tight">
            {offersTitle}
          </h2>
        </div>
      </ScrollReveal>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {offers.map((offer, i) => (
          <ScrollReveal delay={i * 0.15}>
            <div class="bg-[var(--bg-primary)] rounded-2xl p-8 shadow-sm hover:shadow-md transition-shadow border border-[var(--border-color)] flex flex-col h-full">
              <h3 class="text-xl font-bold text-[var(--text-primary)] mb-3">{offer.title}</h3>
              <p class="text-[var(--text-secondary)] leading-relaxed mb-6 flex-grow">{offer.description}</p>
              <a href={`/en/${offer.link}`} class="inline-flex items-center text-[var(--color-green-primary)] font-semibold hover:underline">
                {offer.cta}
              </a>
            </div>
          </ScrollReveal>
        ))}
      </div>
    </div>
  </section>

  <!-- ========== 6. HOW IT WORKS SUMMARY ========== -->
  <section class="py-16 md:py-24 bg-[var(--bg-green)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <ScrollReveal>
        <div class="text-center mb-12 md:mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-[var(--text-primary)] tracking-tight">
            {hiwTitle}
          </h2>
        </div>
      </ScrollReveal>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        {hiwSteps.map((step, i) => (
          <ScrollReveal delay={i * 0.15}>
            <div class="relative text-center">
              <div class="w-16 h-16 rounded-full bg-[var(--color-green-primary)] text-white flex items-center justify-center mx-auto mb-6">
                <span class="text-2xl font-bold">{String(i + 1).padStart(2, '0')}</span>
              </div>
              {i < 3 && (
                <div class="hidden lg:block absolute top-8 left-[calc(50%+2rem)] w-[calc(100%-4rem)] h-0.5 bg-[var(--color-green-primary)]/20" aria-hidden="true" />
              )}
              <h3 class="text-lg font-bold text-[var(--text-primary)] mb-2">{step.title}</h3>
              <p class="text-sm text-[var(--text-secondary)] leading-relaxed">{step.description}</p>
            </div>
          </ScrollReveal>
        ))}
      </div>

      <div class="text-center mt-10">
        <a href="/en/how-it-works" class="inline-flex items-center text-[var(--color-green-primary)] font-semibold hover:underline">
          {hiwCta}
        </a>
      </div>
    </div>
  </section>

  <!-- ========== 7. SOCIAL PROOF ========== -->
  <section class="py-16 md:py-24 bg-[var(--bg-beige)]">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <ScrollReveal>
        <div class="text-center mb-12 md:mb-16">
          <h2 class="text-3xl md:text-4xl font-bold text-[var(--text-primary)] tracking-tight">
            {socialTitle}
          </h2>
        </div>
      </ScrollReveal>

      <ScrollReveal>
        <div class="max-w-3xl mx-auto mb-16">
          <div class="bg-[var(--bg-primary)] rounded-2xl p-8 border border-[var(--border-color)] text-center">
            <p class="text-[var(--text-secondary)] italic">Merchant testimonial placeholder.</p>
          </div>
        </div>
      </ScrollReveal>

      <ScrollReveal delay={0.2}>
        <h3 class="text-xl font-bold text-[var(--text-primary)] text-center mb-8">{partnersSubtitle}</h3>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-3xl mx-auto">
          {techPartners.map((partner) => (
            <div class="bg-[var(--bg-primary)] rounded-xl p-6 text-center border border-[var(--border-color)]">
              <p class="font-semibold text-[var(--text-primary)] text-sm mb-1">{partner.name}</p>
              <p class="text-xs text-[var(--text-secondary)]">{partner.description}</p>
            </div>
          ))}
        </div>
      </ScrollReveal>
    </div>
  </section>

  <!-- ========== 8. MASCOTTE ========== -->
  <div class="bg-[var(--bg-green)]">
    <ScrollReveal>
      <MascotteSection text={mascotteText} ctaText={mascotteCta} ctaHref="#" />
    </ScrollReveal>
  </div>

  <!-- ========== 9. FAQ ========== -->
  <div class="bg-[var(--bg-beige)]">
    <ScrollReveal>
      <FAQ items={faqItems} title={faqTitle} />
    </ScrollReveal>
  </div>

  <!-- ========== 10. PRE-FOOTER CTA ========== -->
  <ScrollReveal>
    <CTASection
      title={finalCtaTitle}
      subtitle={finalCtaSubtitle}
      ctaText={finalCtaPrimary}
      ctaHref="/en/contact"
      secondaryCtaText={finalCtaSecondary}
      secondaryCtaHref="/en/contact"
    />
  </ScrollReveal>
</PageLayout>
